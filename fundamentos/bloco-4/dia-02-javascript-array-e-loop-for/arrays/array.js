let cakes = ["Chocolate", "Formigueiro", "Limão", "Fubá cremoso", "Milho", "Cenoura"];
// console.log(cakes);

// se usarmos .length ele irá imprimir o número de itens dentro do array
// console.log(cakes.length);

// para adicionar um item ao array você pode fazer por exemplo, cakes[6] = "Creme"; 
// mas temos uma maneira mais fácil para colocar um item no array, usando o .push
cakes.push("Creme");

// Lembrando que dentro de um array, as posições começam pelo índice 0.
// Conseguimos trazer um único elemento do array usando [indice]:
// Por exemplo, console.log(cakes[1]) // e isso nos traria o índice 1 do array: "Formigueiro"

// Usando o método .sort, nós conseguimos exibir no log, os item do array em ordem alfabética.
// console.log(cakes.sort)
console.log(cakes);

